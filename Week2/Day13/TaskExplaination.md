### Task 13.1 problem Explaination

we used open() as f to open a file where we donot need to think about closing it manually

### Task 13.2 problem Explaination

action should not be 'w' instead of that 'a' will be the action.

### Task 13.3 problem Explaination

image are usually in binary format

### Task 13.4 problem Explaination

We can handle UnicodeDecodeError using try except block

### Task 13.5 problem Explaination

To convert the dictionary for file we use json.dumps()

### Task 13.6 problem Explaination

We can read a csv into a dictionary using `csv.DictReader()`

### Task 13.7 problem Explaination

Python use a intermidiate state called Buffer before flushing the data into the disk. When the buffer is full then it usually flush it.

### Task 13.8 problem Explaination

Using Pathlib.Path we can join path os invariantly.

### Task 13.9 problem Explaination

We have created a context manger for Timer which can measure the time takes in a block

### Task 13.10 problem Explaination

We can save class pickle file using pickle.dump()
